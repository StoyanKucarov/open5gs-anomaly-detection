# Strip out noisy Kubernetes metadata headers
promtail:
  pipelineStages:
    - docker: {}
    - regex:
        expression: "^(?P<time>\\S+) (?P<stream>stdout|stderr) (?P<flags>\\S+) (?P<content>.*)$"
    - output:
        source: content